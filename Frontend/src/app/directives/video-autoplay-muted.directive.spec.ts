import { VideoAutoplayMutedDirective } from './video-autoplay-muted.directive';

describe('VideoAutoplayMutedDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoAutoplayMutedDirective();
    expect(directive).toBeTruthy();
  });
});
