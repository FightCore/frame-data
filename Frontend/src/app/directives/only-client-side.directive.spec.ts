import { OnlyClientSideDirective } from './only-client-side.directive';

describe('OnlyClientSideDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyClientSideDirective();
    expect(directive).toBeTruthy();
  });
});
